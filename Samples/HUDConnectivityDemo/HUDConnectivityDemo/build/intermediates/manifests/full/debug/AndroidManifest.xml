<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reconinstruments.demo.hudconnectivity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.reconinstruments.demo.hudconnectivity.TestActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.reconinstruments.demo.hudconnectivity.WebImageActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.reconinstruments.demo.hudconnectivity.DownloadFileActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.reconinstruments.demo.hudconnectivity.UploadFileActivity"
            android:label="@string/app_name" />

        <!-- Comment these 2 uses-library lines out if you want to to install on your phone -->
        <uses-library android:name="com.reconinstruments.lib" />
        <uses-library android:name="com.reconinstruments.os" />
    </application>

</manifest>